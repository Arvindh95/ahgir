services:
  postgres:
    ports: !reset []  # do not publish; access only via internal docker network

  minio:
    ports: !reset []  # do not publish; access only via Caddy /storage proxy

  redis:
    ports: !reset []  # do not publish

  backend:
    volumes: !reset []
    # Trust forwarded headers ONLY from the docker bridge gateway. Edge nginx
    # already overwrites X-Forwarded-For with the real client IP from
    # CF-Connecting-IP, so the stack rejects attacker-supplied XFF that
    # bypasses the edge. Adjust the IP list to match your bridge network.
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 4 --proxy-headers --forwarded-allow-ips=172.18.0.1,127.0.0.1
    ports: !reset []  # do not publish; Caddy proxies /api → backend:8000
    environment:
      COMPREFACE_API_URL: "http://compreface-api:8080,http://compreface-api-2:8080"
    depends_on:
      compreface-api-2:
        condition: service_started

  worker:
    volumes: !reset []
    environment:
      COMPREFACE_API_URL: "http://compreface-api:8080,http://compreface-api-2:8080"
    depends_on:
      compreface-api-2:
        condition: service_started

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        BUILD_ENV: production
        NEXT_PUBLIC_API_URL: /api
    volumes: !reset []
    ports: !reset []  # do not publish; Caddy proxies /* → frontend:3000
    command: ["npm", "start"]

  # CompreFace admin disabled in prod (saves ~660MB RAM); also no port pub
  compreface-admin:
    ports: !reset []
    deploy:
      replicas: 0

  # Tune CompreFace API memory (8g -> 2g) and unpublish port (internal-only in prod)
  compreface-api:
    ports: !reset []
    environment:
      API_JAVA_OPTS: "-Xmx2g"

  # Second CompreFace instance for load balancing
  compreface-api-2:
    image: exadel/compreface-api:1.2.0
    container_name: compreface-api-2
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${COMPREFACE_DB_PASSWORD:-postgres}
      POSTGRES_URL: jdbc:postgresql://compreface-postgres-db:5432/frs
      SPRING_PROFILES_ACTIVE: dev
      API_JAVA_OPTS: "-Xmx2g"
      SAVE_IMAGES_TO_DB: "true"
      MAX_FILE_SIZE: "50MB"
      MAX_REQUEST_SIZE: "50MB"
      CONNECTION_TIMEOUT: "10000"
      READ_TIMEOUT: "60000"
    depends_on:
      compreface-postgres-db:
        condition: service_healthy
      compreface-core-2:
        condition: service_started

  compreface-core-2:
    image: exadel/compreface-core:1.2.0
    container_name: compreface-core-2
    environment:
      ML_PORT: "3000"
      ML_PLUGINS_LIST: "calculator,gender"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/healthcheck"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s
